ps is a shell command that outputs list of processes which running at computer. 
can be used with parameters:
   e               outputs all processes
   -U <username>   outputs processes running by specified user
   -m              outputs processes running sorted by memory usage
   -r              outputs processes running sorted by CPU usage
